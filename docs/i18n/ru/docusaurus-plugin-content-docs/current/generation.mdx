---
title: "Генерация и расписание"
description: "Сведения о генерации файлов фидов для дальнейшего распространения"
type: guide
status: stable
since: "1.0"
keywords: ["generation", "schedule", "queue", "targets", "feed:generate"]
---

[← Предыдущая](./create-feeds.mdx) · [Назад к README](https://github.com/TheDragonCode/laravel-feeds#readme) · [Следующая →](./supported-formats.mdx)

# Генерация и расписание

Чтобы сгенерировать все активные фиды, используйте консольную команду:

```bash
php artisan feed:generate
```

В результате будут выполнены все активные фиды (`is_active = true`) из таблицы `feeds` или из таблицы, указанной в файле `config/feeds.php`.

:::tip
Обратите внимание: если вызвать команду без параметра, консоль покажет ссылки на все фиды, в том числе со свойством `is_active`, равным `false`.

Но такие фиды не будут выполнены и получат статус `SKIP`.
:::

## Один фид {/* #single_feed */}

:::warning
Указанный фид **будет выполнен**, даже если он отключён в таблице `feeds` базы данных.
:::

Чтобы сгенерировать определённый фид, вызовите консольную команду `feed:generate` и передайте ей идентификатор фида из таблицы `feeds`:

```bash
php artisan feed:generate 123
```

## Выбор цели {/* #target_selection */}

Для [целевого фида](./create-feeds.mdx#targeted_feeds) идентификатор записи выбирает класс фида, а ключ цели — один вариант вывода.

Запуск без `--target` лениво обрабатывает `targets()` и генерирует все цели:

```bash
php artisan feed:generate 123
```

Передайте повторяемый параметр `--target`, чтобы сгенерировать только выбранные цели:

```bash
php artisan feed:generate 123 --target=42
php artisan feed:generate 123 --target=42 --target=81
```

Явно заданные ключи используют `findTarget()` вместо перечисления `targets()`. Повторяющиеся ключи обрабатываются один раз в порядке первого появления. Все запрошенные ключи разрешаются до начала генерации.

Для `--target` требуется один идентификатор записи фида. Команда отклоняет пустые и неизвестные ключи, а также выбор цели для фида, который не реализует `HasFeedTargets`.

Полное перечисление целей выполняется потоково, а не атомарно. Если `targets()` завершится ошибкой после возврата нескольких значений, предыдущие цели уже могут быть сгенерированы или поставлены в очередь. Пустой итерируемый объект ничего не генерирует и не создаёт неявную цель по умолчанию.

## Вывод для ИИ-агентов {/* #ai_agent_output */}

Когда `feed:generate` выполняется внутри ИИ-агента для работы с кодом, распознанного [Laravel Agent Detector](https://github.com/laravel/agent-detector), команда автоматически выводит один объект JSON вместо индикаторов прогресса и форматированных строк состояния. Дополнительный параметр команды не требуется. Вне распознанного агента обычный консольный вывод не меняется.

При прямой генерации каждый созданный фид содержит список всех опубликованных файлов и количество записей в каждом из них:

```json
{
  "tool": "feed:generate",
  "result": "success",
  "feeds": [
    {
      "class": "App\\Feeds\\SitemapFeed",
      "status": "generated",
      "files": [
        {
          "path": "/var/www/html/storage/app/public/sitemap.xml",
          "records": 1250
        }
      ]
    },
    {
      "class": "App\\Feeds\\DisabledFeed",
      "status": "skipped"
    }
  ]
}
```

Команда выводит компактный JSON одной строкой. В примере он отформатирован только для удобства чтения.

| Статус | Значение | Дополнительные поля |
| --- | --- | --- |
| `generated` | Файлы фида созданы напрямую. | `files` с полями `path` и `records` для каждого файла |
| `queued` | Режим очереди включён, и задание фида выполнено синхронно для агента. | Нет |
| `skipped` | Фид неактивен, а команда выполнена без идентификатора фида. | Нет |

Каждая целевая запись `generated` или `queued` также содержит поле `target` с выбранным ключом.

:::note
В режиме очереди ветка для агента синхронно выполняет каждое активное задание перед возвратом результата JSON. Обычная ветка с выводом для человека продолжает отправлять задания через настроенное подключение к очереди.
:::

Эта оболочка JSON используется только при успешном выполнении. Если генерация завершается исключением, Laravel обрабатывает ошибку команды обычным способом и не выводит оболочку ошибки JSON.

## Очередь {/* #queue */}

По умолчанию генерация фида выполняется в консольном процессе. Для фидов, требующих много времени или памяти, включите режим Laravel Queue в своём окружении:

```bash
FEED_QUEUE_ENABLED=true
FEED_QUEUE_CONNECTION=redis
FEED_QUEUE_NAME=feeds
FEED_QUEUE_UNIQUE_TTL=3600
```

Когда режим очереди включён, `feed:generate` отправляет по одной задаче для каждого обычного активного фида и каждой выбранной цели целевого фида. При асинхронном подключении команда завершается после отправки задач. Запустите [обработчик очереди](https://laravel.com/docs/queues#running-the-queue-worker) для выбранных подключения и очереди, чтобы обработать эти задачи.

- `FEED_QUEUE_CONNECTION` выбирает подключение к очереди. Если значение не задано, используется `QUEUE_CONNECTION`, а затем `database`.
- `FEED_QUEUE_NAME` выбирает очередь в этом подключении. Не задавайте значение, чтобы использовать очередь подключения по умолчанию.
- `FEED_QUEUE_UNIQUE_TTL` задаёт максимальное время жизни блокировки уникальности в секундах. Обычные задания уникальны по классу фида. Целевые задания уникальны по классу фида и ключу цели; параметры цели не меняют этот идентификатор.

:::note
Подключение `sync` сразу выполняет каждую задачу в консольном процессе. Выберите асинхронное подключение, чтобы перенести генерацию в обработчик. Задайте `FEED_QUEUE_ENABLED=false`, чтобы генерировать фиды напрямую, без отправки задач.
:::

## Расписание {/* #schedule */}

### Автоматическое {/* #automatic */}

Чтобы автоматизировать генерацию фидов, используйте [планировщик](https://laravel.com/docs/scheduling#running-the-scheduler). Для этого добавьте вызов вспомогательного класса в файл `routes/console.php`, если стандартный путь не был изменён:

```php source="docs/snippets/schedule-setup.php" lines="5-"
use DragonCode\LaravelFeed\Helpers\ScheduleFeedHelper;
use Illuminate\Support\Facades\Schedule;

ScheduleFeedHelper::register();

Schedule::call(function () {
    // ... other action
})->everySecond();
```

Так регистрируются все активные фиды по расписанию из столбца `expression` базы данных. Целевой фид в расписании генерирует все значения, возвращённые `targets()`.

### Ручное {/* #manual */}

Расписание также можно задать напрямую по собственным правилам. В этом случае будут выполнены только указанные вами фиды.

```php source="docs/snippets/schedule-setup-manual.php" lines="5-"
use DragonCode\LaravelFeed\Commands\FeedGenerateCommand;
use Illuminate\Support\Facades\Schedule;

Schedule::command(FeedGenerateCommand::class, [111])
    ->withoutOverlapping()
    ->runInBackground()
    ->daily();

Schedule::command(FeedGenerateCommand::class, [222])
    ->withoutOverlapping()
    ->runInBackground()
    ->hourly();

Schedule::call(function () {
    // ... other action
})->everySecond();
```

## Смотрите также

- [Создание фидов](./create-feeds.mdx)
- [События](./events.mdx)
- [Расположение](./location.mdx)
