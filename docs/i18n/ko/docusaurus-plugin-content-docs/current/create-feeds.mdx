---
title: "피드 생성"
description: "피드, 피드 항목 및 피드 정보 클래스를 생성하고 채우는 방법"
type: guide
status: stable
since: "1.0"
keywords: ["feed", "FeedItem", "FeedInfo", "FeedTarget", "make:feed"]
---

import ThemedImage from '@theme/ThemedImage';

[← 이전](./installation.mdx) · [README로 돌아가기](https://github.com/TheDragonCode/laravel-feeds#readme) · [다음 →](./generation.mdx)

# 피드 생성

:::note
필요한 클래스를 만드는 가장 편리한 방법은 [Laravel Idea](https://laravel-idea.com) 플러그인을 사용하는 것입니다. 이 플러그인은 [PhpStorm](https://www.jetbrains.com/phpstorm/)용입니다.

<ThemedImage
  alt="Laravel Idea"
  sources={{
    light: '/img/laravel-idea.png',
    dark: '/img/laravel-idea_dark.png',
  }}
/>
:::

피드 클래스를 만들려면 `make:feed` 콘솔 명령을 사용하십시오.

```bash
php artisan make:feed <name>
```

이 명령은 피드 파일을 생성합니다. 예: `app/Feeds/UserFeed.php`.

이 콘솔 명령은 피드 클래스와 함께 항목 및 정보 클래스도 만들 수 있습니다. `--item`과 `--info` 옵션을 사용하십시오.

```bash
# Create a feed class and info class
php artisan make:feed <name> --info

# Create a feed class and item class
php artisan make:feed <name> --item

# Create a feed class, item class, and info class
php artisan make:feed <name> --item --info
```

단축 옵션도 사용할 수 있습니다.

```bash
# Create a feed class and item class
php artisan make:feed <name> -t

# Create a feed class and info class
php artisan make:feed <name> -i

# Create a feed class, item class, and info class
php artisan make:feed <name> -it
```

:::note
피드를 만들면 데이터베이스에 피드를 추가하기 위한 operation 또는 마이그레이션도 생성됩니다.

프로젝트가 [**Laravel Deploy Operations**](https://deploy-operations.dragon-code.pro)를 사용하면 operation 클래스가 생성되고, 그렇지 않으면 마이그레이션 클래스가 생성됩니다.

이는 데이터베이스에 피드 정보를 추가하고 관리하는 데 필요합니다.
:::

## 피드 채우기 {/* #fill_feeds */}

### 피드 {/* #feed */}

기본 피드 클래스를 채우십시오. 예:

```php source="docs/snippets/create-feeds-feed.php" lines="5-"
namespace App\Feeds;

use App\Feeds\Items\UserFeedItem;
use App\Models\User;
use DragonCode\LaravelFeed\Feeds\Feed;
use DragonCode\LaravelFeed\Feeds\Items\FeedItem;
use Illuminate\Database\Eloquent\Builder;
use Illuminate\Database\Eloquent\Model;

class UserFeed extends Feed
{
    public function builder(): Builder
    {
        return User::query()
            ->whereNotNull('email_verified_at')
            ->where('created_at', '>', now()->subYear());
    }

    public function item(Model $model): FeedItem
    {
        return new UserFeedItem($model);
    }
}
```

#### 피드 항목 {/* #feed_item */}

피드 항목 클래스를 채우십시오. 예:

```php source="docs/snippets/generation-feed-item.php" lines="5-"
namespace App\Feeds\Items;

use DragonCode\LaravelFeed\Feeds\Items\FeedItem;

/** @property-read \App\Models\User $model */
class UserFeedItem extends FeedItem
{
    public function toArray(): array
    {
        return [
            'name'  => $this->model->name,
            'email' => $this->model->email,
        ];
    }
}
```

##### 선택적 값 {/* #optional_values */}

값을 사용할 수 없을 때 필드를 생략하려면 해당 값으로 `DragonCode\LaravelFeed\Data\OptionalData` 인스턴스를 반환하십시오.

```php source="docs/snippets/optional-values.php" lines="5-"
namespace App\Feeds\Items;

use DragonCode\LaravelFeed\Data\OptionalData;
use DragonCode\LaravelFeed\Feeds\Items\FeedItem;

class UserFeedItem extends FeedItem
{
    public function toArray(): array
    {
        return [
            'id'   => $this->model->id,
            'name' => $this->model->name,

            'cityId' => $this->model->city?->id ?? new OptionalData,

            'createdAt' => $this->model->created_at,
            'updatedAt' => $this->model->updated_at ?? new OptionalData,
        ];
    }
}
```

선택적 필드는 일반 중첩 필드를 포함하여 JSON 및 JSON Lines 객체와 XML 및 RSS 요소에서 제거됩니다. CSV는 열 위치를 유지하고 빈 필드를 기록합니다.

애플리케이션에서 [Spatie Laravel Data](https://spatie.be/docs/laravel-data/v4/as-a-data-transfer-object/optional-properties)를 사용한다면 대신 `Spatie\LaravelData\Optional`을 반환할 수 있습니다. 두 선택적 값 클래스는 모두 자동으로 처리됩니다.

```php
use Spatie\LaravelData\Optional;

'updatedAt' => $this->model->updated_at ?? Optional::create(),
```

## 대상별 피드 {/* #targeted_feeds */}

등록된 피드 클래스 하나에서 파트너, 스토어, 로케일 또는 애플리케이션이 정의한 다른 대상마다 독립적인 출력을 생성해야 할 때 대상별 피드를 사용하십시오. `feeds` 테이블에는 해당 피드 클래스의 행이 하나만 유지됩니다.

`HasFeedTargets`를 구현하십시오. 패키지에 포함된 `InteractsWithFeedTargets` 트레이트는 표준 `forTarget()` 및 `target()` 동작을 제공하며, 피드 클래스는 지연 열거와 직접 조회를 구현합니다.

```php source="docs/snippets/targeted-feed.php" lines="5-"
namespace App\Feeds;

use App\Models\Partner;
use App\Models\Product;
use DragonCode\LaravelFeed\Concerns\InteractsWithFeedTargets;
use DragonCode\LaravelFeed\Contracts\HasFeedTargets;
use DragonCode\LaravelFeed\Feeds\Feed;
use DragonCode\LaravelFeed\Feeds\FeedTarget;
use Illuminate\Database\Eloquent\Builder;

class PartnerFeed extends Feed implements HasFeedTargets
{
    use InteractsWithFeedTargets;

    public function targets(): iterable
    {
        foreach (Partner::query()->select('id')->lazyById() as $partner) {
            yield $this->makeTarget($partner);
        }
    }

    public function findTarget(string $key): ?FeedTarget
    {
        $partner = Partner::query()->find($key);

        return $partner ? $this->makeTarget($partner) : null;
    }

    public function builder(): Builder
    {
        return Product::query()->where(
            'partner_id',
            $this->target()->parameters['partner_id'],
        );
    }

    public function filename(): string
    {
        return "partners/{$this->target()->key}.xml";
    }

    private function makeTarget(Partner $partner): FeedTarget
    {
        return new FeedTarget(
            key       : (string) $partner->getKey(),
            parameters: ['partner_id' => $partner->getKey()],
        );
    }
}
```

- `targets()`는 설정되지 않은 피드 정의에서 호출되며, 명령이나 스케줄이 모든 대상을 생성할 때 지연 방식으로 소비됩니다.
- `findTarget()`은 명시적인 각 `--target` 키를 대소문자를 구분하여 조회합니다. 키가 없으면 `null`을 반환하고, 있으면 요청한 키를 가진 대상을 반환하십시오.
- `FeedTarget::$key`는 비어 있지 않고 안정적이며 피드 클래스 안에서 고유해야 하고 외부에 노출해도 안전해야 합니다. 키는 정규화되거나 정제되지 않으며 콘솔 출력, 에이전트 JSON, 이벤트 및 오류 컨텍스트에 나타납니다.
- `FeedTarget::$parameters`는 큐 작업에 저장되는 애플리케이션 소유의 스냅샷입니다. 패키지는 이 값을 해석하지 않습니다. 작고 직렬화 가능하게 유지하고 비밀 정보를 넣지 마십시오. Eloquent 모델 대신 식별자를 저장하십시오.
- `target()`은 `builder()`, `filename()` 및 기타 실행 훅 안에서 활성 대상을 반환합니다. 피드가 `forTarget()`으로 설정되기 전에는 `LogicException`을 발생시킵니다.

:::warning
각 대상은 서로 다른 출력 파일 이름으로 해석되어야 합니다. 그렇지 않으면 나중 대상이 앞선 대상의 출력을 덮어씁니다. 애플리케이션이 형식을 제어하는 경우에만 경로에 대상 키를 사용하십시오.
:::

일반적인 파일 분할은 대상마다 따로 적용됩니다. 예를 들어 `partners/42.xml`은 `partners/42-1.xml`, `partners/42-2.xml` 등의 파일이 됩니다.

일반 피드 클래스는 `HasFeedTargets`를 구현하지 않으며 변경할 필요가 없습니다.

## 파일 형식 {/* #file_format */}

:::tip
사용 가능한 형식 목록은 [지원 형식](./supported-formats.mdx) 페이지에서 확인할 수 있습니다.
:::

피드는 여러 형식으로 생성할 수 있습니다. 피드 클래스는 의도된 형식으로만 올바르게 내보낼 수 있습니다. 이는 [디렉티브](./directives.mdx)와 기타 형식별 특성 때문입니다.

필요한 형식을 적용하려면 피드 클래스의 `$format` 속성을 재정의하십시오.

```php source="docs/snippets/create-feeds-feed-format.php" lines="5-"
namespace App\Feeds;

use DragonCode\LaravelFeed\Enums\FeedFormatEnum;
use DragonCode\LaravelFeed\Feeds\Feed;
use Illuminate\Database\Eloquent\Builder;

class UserFeed extends Feed
{
    protected FeedFormatEnum $format = FeedFormatEnum::Json;

    public function builder(): Builder
    {
        //
    }
}
```

## 관련 문서

- [생성 및 스케줄](./generation.mdx)
- [지원 형식](./supported-formats.mdx)
- [프리셋](./presets.mdx)
